# Internship Task -- Landing Page

A responsive landing page built as part of an internship task using
**React, TypeScript, Vite, and Tailwind CSS**.

## 🚀 Live Demo

[View Live Demo](https://intership-task-eight.vercel.app/)

## 📌 Project Overview

This project is a modern, responsive landing page developed with a
component-based React architecture. The application is organized into
reusable components, layouts, sections, routes, hooks, utilities, and
styling modules.

The project focuses on:

-   Responsive UI design
-   Reusable React components
-   Clean project structure
-   Type-safe development with TypeScript
-   Utility-first styling with Tailwind CSS
-   Client-side routing
-   Modern Vite development workflow

## 🛠️ Tech Stack

-   **React 19**
-   **TypeScript**
-   **Vite**
-   **Tailwind CSS 4**
-   **React Router DOM**
-   **React Icons**
-   **Oxlint**
-   **Prettier**
-   **Vercel** for deployment

## ✨ Features

-   Responsive landing page
-   Mobile-friendly navigation
-   Reusable UI components
-   Structured page sections
-   Client-side routing
-   Tailwind CSS based styling
-   React Icons integration
-   TypeScript for type safety
-   Optimized Vite development and production builds

## 📂 Project Structure

``` text
landing-page/
├── public/
├── src/
│   ├── Routes/
│   ├── assets/
│   ├── components/
│   ├── hooks/
│   ├── layouts/
│   ├── pages/
│   ├── sections/
│   ├── styles/
│   ├── utils/
│   ├── App.css
│   ├── App.tsx
│   ├── index.css
│   └── main.tsx
├── .gitignore
├── .oxlintrc.json
├── .prettierrc
├── index.html
├── package.json
├── package-lock.json
├── tsconfig.json
└── vite.config.ts
```

## 💻 Getting Started

### Prerequisites

Make sure you have the following installed:

-   [Node.js](https://nodejs.org/)
-   npm

### Installation

Clone the repository:

``` bash
git clone https://github.com/SSR14FEB/intershipTask.git
```

Navigate to the landing page project:

``` bash
cd intershipTask/landing-page
```

Install dependencies:

``` bash
npm install
```

### Run Development Server

``` bash
npm run dev
```

The application will be available at the local Vite development URL
shown in your terminal.

## 📦 Available Scripts

  Command             Description
  ------------------- -----------------------------------
  `npm run dev`       Start the Vite development server
  `npm run build`     Create a production build
  `npm run preview`   Preview the production build
  `npm run lint`      Run Oxlint

## 🏗️ Build for Production

Create an optimized production build with:

``` bash
npm run build
```

To preview the production build locally:

``` bash
npm run preview
```

## 🎨 Styling

The project uses **Tailwind CSS 4** with the Vite integration. Styling
is primarily handled through utility classes, allowing responsive
layouts and reusable UI patterns without relying heavily on custom CSS.

## 📱 Responsive Design

The landing page is designed to work across different screen sizes,
including:

-   Desktop
-   Laptop
-   Tablet
-   Mobile

Responsive Tailwind utilities are used to adapt layouts, typography,
spacing, and navigation across breakpoints.

## 🚀 Deployment

The project is deployed on **Vercel**.

**Live:** https://intership-task-eight.vercel.app/

## 👨‍💻 Author

**SSR14FEB**

GitHub: https://github.com/SSR14FEB

## 📄 License

This project was created for an internship task and is available for
learning and portfolio purposes.
